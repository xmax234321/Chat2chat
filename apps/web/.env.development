# Dev-only relay override (never used in production builds).
# VITE_CHAT2CHAT_DEV_RELAY_HTTP=http://localhost:3847
# VITE_CHAT2CHAT_DEV_RELAY_WS=ws://localhost:3847/ws

# Local relay via Vite proxy (alternative):
# VITE_USE_LOCAL_RELAY=1
